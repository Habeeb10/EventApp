import { StyleSheet } from "react-native";
import * as colors from "../../common/colors";

export const Reduxstyles = StyleSheet.create({
  edit: {
    height: 50,
    width: 150,
    backgroundColor: "skyblue",
    alignItems: "center",
    justifyContent: "center",
    alignSelf: "center",
    marginTop: 50,
    borderRadius: 10,
  },
});
